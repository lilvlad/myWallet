password: 123456

Too see the SHA-1 Key run this commands:
keytool -keystore debug.keystore -list -v
keytool -keystore my-upload-key.keystore -list -v

alias name: my-key-alias
store file name: my-upload-key.keystore